.container.well
  .row
    .col-sm-6.col-sm-offset-3.stylish-input-group
      = search_form_for query do |f|
        = f.search_field searchable_fields, class: 'form-control', placeholder: t('defaults.search')
        = button_tag(type: 'submit') do
          %span.glyphicon.glyphicon-search